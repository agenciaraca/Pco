import Placeholder from '../../components/Placeholder';

export default function AdminLoginCustomize() {
  return (
    <Placeholder
      title="Customizar Login"
      subtitle="Logo, título, subtítulo, frase, imagem/vídeo de fundo, cores e tipografia."
      description="Personalize cada aspecto da tela de login (logo, frase institucional, cores, gradiente, posição do formulário, modo claro/escuro). Persistência em mock state nesta fase."
    />
  );
}
