import Placeholder from '../../components/Placeholder';

export default function AdminCertificates() {
  return (
    <Placeholder
      title="Certificados — Admin"
      subtitle="Modelos, emitidos, validação, reemissão e QR Code."
      description="Gestão completa de modelos de certificado, lista de emitidos por aluno/curso, validação por código e reemissão controlada."
    />
  );
}
